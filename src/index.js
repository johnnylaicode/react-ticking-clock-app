/*==================================================
/src/index.js
==================================================*/
import React from 'react';
import ReactDOM from 'react-dom';
import App from "./App.js";

// Render React element in HTML DOM "root" element node
ReactDOM.render(
  <App />,
  document.getElementById('root')
);